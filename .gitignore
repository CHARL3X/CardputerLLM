.pio/
.pioenvs/
.piolibdeps/
.vscode/
*.tmp
*.swp
.DS_Store
build/
# dist/ holds the SD-card-loadable .bin. Not committed; rebuild produces it.
dist/

# dist/sd/ holds the credential files that get copied onto the device's SD
# card. Plaintext secrets in there. Never committed.
# (top-level dist/ rule above already covers it; this comment is a flag.)
